<script>
  import Error from '$lib/StillLife/Error.svelte';
  import Cover from '$lib/StillLife/Cover.svelte';
  import Title from '$lib/StillLife/Title.svelte';
  import BulletedListItem from '$lib/StillLife/BulletedListItem.svelte';
  import Callout from '$lib/StillLife/Callout.svelte';
  import ChildPage from '$lib/StillLife/ChildPage.svelte';
  import Code from '$lib/StillLife/Code.svelte';
  import ColumnList from '$lib/StillLife/ColumnList.svelte';
  import Column from '$lib/StillLife/Column.svelte';
  import Divider from '$lib/StillLife/Divider.svelte';
  import Embed from '$lib/StillLife/Embed.svelte';
  import Header from '$lib/StillLife/Header.svelte';
  import Image from '$lib/StillLife/Image.svelte';
  import IndentGroup from '$lib/StillLife/IndentGroup.svelte';
  import Underline from '$lib/StillLife/Underline.svelte';
  import InlineCode from '$lib/StillLife/InlineCode.svelte';
  import InlineColor from '$lib/StillLife/InlineColor.svelte';
  import Equation from '$lib/StillLife/Equation.svelte';
  import Link from '$lib/StillLife/Link.svelte';
  import NumberedListItem from '$lib/StillLife/NumberedListItem.svelte';
  import Paragraph from '$lib/StillLife/Paragraph.svelte';
  import Quote from '$lib/StillLife/Quote.svelte';
  import TodoItem from '$lib/StillLife/TodoItem.svelte';
  import Toggle from '$lib/StillLife/Toggle.svelte';
  import { setContext } from 'svelte';

  setContext('pageStuff', {
    pageId: '0edcc205aa974a3aa4d6bdefc804ba58',
    title: 'Forgiveness',
    previous: '/bookends',
    next: '/malespero'
  });
</script>

<svelte:head>
  <title>Forgiveness</title>
</svelte:head>

<Cover coverURL={''} iconEmojiOrURL={'undefined'} />

<Title>Forgiveness</Title>

<Code
  code={`recognizes &lt;em&gt;Time Passed&lt;/em&gt; as irretrievable
  understanding the fiction of self-styled facts:
  &lt;em&gt;Reality&lt;/em&gt;, shattered into unreliable memory
  shredded to bits of scattered confetti
  some few fragments of Complexity compressed
  woven by dreams into neural nets
  (stored, let’s be honest, with limited shelf space!)
  hung up in consciousness from time to time like a tapestry
  simpler, and easier to understand than what &lt;em&gt;actually was&lt;/em&gt;

comes be&lt;em&gt;fore&lt;/em&gt; the offense
  it takes the tapestry down
  stitches over the wound
  writes the villain out of the story
  (else, it was not &lt;em&gt;for&lt;/em&gt;giveness, but a painful facsimile)

lifts the heart
  for it is a kindness to give
  more so to &lt;em&gt;give completely&lt;/em&gt;
  letting go of expectations without letting go of hope

gives more than what was taken
  knowing that the only fact lies in a question
	  &lt;em&gt;How could you have done otherwise?&lt;/em&gt;

fails to pretend that the one who caused the harm still exists, unchanged
  cradles curiosity like a newborn
  asking, not telling
	  &lt;em&gt;Who do you want to be now?&lt;/em&gt;

■`}
  language="plain text"
/><Paragraph
  blockProps={{
    pageId: '0edcc205aa974a3aa4d6bdefc804ba58',
    id: '244cfea9-fb60-450e-86d8-18072e7a45cd',
    created_time: '2022-12-13T22:09:00.000Z',
    last_edited_time: '2022-12-13T22:09:00.000Z'
  }}
/>
